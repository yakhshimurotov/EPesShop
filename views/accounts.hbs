<div class="container">
  <h1 class="mb-5">Account's</h1>
  <div
    id="loader"
    class="loader"
    style="position: fixed; inset: 0; color: white; font-size: 2rem; display: flex; justify-content: center; align-items: center; z-index: 9999;"
  >
    <div class="spinner-border loader-obj" role="status">
      <span class="visually-hidden">Loading...</span>
    </div>
  </div>
  {{#if product.length}}
    <div class="row row-cols-1 row-cols-sm-2 row-cols-md-2 g-4 p-3 card-box">
      {{#each product}}
        <div class="col">
          <div class="card card-in">
            <img
              src="/uploads/{{this.image}}"
              alt="product image"
              width="100%"
              class="img-thumbnail"
              data-bs-toggle="modal"
              data-bs-target="#imageModal{{@index}}"
            />

            <div class="card-body">
              <h5 class="text-bold fw-bold">{{this.title}}</h5>
              <p class="card-text">{{this.description}}</p>
              <div class="d-flex justify-content-left align-items-center gap-3">
                <p class="mb-3 badge bg-success-subtle border border-success-subtle text-success-emphasis rounded-pill">{{getFullNameCharacter user.firstName}}.{{getFullNameCharacter user.lastName}}</p>
                <p class="mb-3 badge bg-warning-subtle border border-warning-subtle text-warning-emphasis rounded-pill score">⭐ {{this.scores}}</p>
                <span class="mb-3 badge bg-primary-subtle border border-primary-subtle text-primary-emphasis rounded-pill">{{this.uzs}} so'm 💵</span>
              </div>
              <div class="d-flex justify-content-between align-items-center">
                <div class="btn-group">
                  <div
                    class="btn-group-sm"
                    role="group"
                    aria-label="Basic example">
                    {{#if @root.token}}
                      <a class="btn btn-dark btn-sm" href="{{this.url}}">Message</a>
                      {{#ifequal this.user._id @root.userId}}
                        <button type="button" class="btn btn-dark">Edit</button>
                        <button
                          type="button"
                          class="btn btn-dark"
                        >Delete</button>
                      {{/ifequal}}
                      {{else}}
                       <a class="btn btn-dark btn-sm" href="{{this.url}}">Message</a>
                    {{/if}}
                  </div>
                </div>
                <span class="badge bg-secondary-subtle border border-secondary-subtle text-secondary-emphasis rounded-pill time">{{formatData cratedAt}}</span>
              </div>
            </div>
          </div>
        </div>

        <!-- Modal -->
        <div
          class="modal fade"
          id="imageModal{{@index}}"
          tabindex="-1"
          aria-labelledby="imageModalLabel{{@index}}"
          aria-hidden="true"
        >
          <div class="modal-dialog modal-dialog-centered modal-lg">
            <div class="modal-content">
              <div class="modal-body text-center">
                <img
                  src="/uploads/{{this.image}}"
                  class="img-fluid"
                  alt="Full image"
                />
              </div>
            </div>
          </div>
        </div>
      {{/each}}
    </div>
  {{else}}
    <p>No Product 👀</p>
  {{/if}}
 <div class="d-flex justify-content-center mt-4">
  <nav aria-label="Page navigation">
    <ul class="pagination pagination-dark salom">
      {{#if (gt currentPage 1)}}
        <li class="page-item">
          <a class="page-link bg-dark text-white border-secondary" href="/accounts?page={{subtract currentPage 1}}">
            « Previous
          </a>
        </li>
      {{/if}}

      {{#each (range 1 totalPages)}}
        <li class="page-item {{#ifEquals this ../currentPage}}{{/ifEquals}}">
          <a class="page-link {{#ifEquals this ../currentPage}}bg-light text-dark{{else}}bg-dark text-white border-secondary{{/ifEquals}}" 
             href="/accounts?page={{this}}">
            {{this}}
          </a>
        </li>
      {{/each}}

      {{#if (lt currentPage totalPages)}}
        <li class="page-item">
          <a class="page-link bg-dark text-white border-secondary" href="/accounts?page={{add currentPage 1}}">
            Next »
          </a>
        </li>
      {{/if}}
    </ul>
  </nav>
</div>


</div>